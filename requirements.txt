# Core (align with repo root where possible)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
torch>=2.1.0

# PyTorch Geometric (GNNs)
# Install after torch: pip install torch_geometric
torch_geometric>=2.4.0

# Optional: faster PyG ops (uncomment and set TORCH/CUDA for your env)
# pyg_lib torch_scatter torch_sparse -f https://data.pyg.org/whl/torch-2.1.0+cpu.html

# Plotting (for optional visualizations)
matplotlib>=3.7.0
seaborn>=0.13.0
networkx>=3.0
